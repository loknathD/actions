name: My First Workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "my sample github actions code"

        - name: list the file
          run: ls

        - name: read the flie
          run: cat README.md
          
